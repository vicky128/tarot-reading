{
  "name": "tarot-worker",
  "version": "1.0.0",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "npm install --omit=dev && wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.112.0"
  }
}
